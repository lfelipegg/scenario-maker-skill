Use the explicitly selected Scenario Maker skill in ./selected-skill/SKILL.md.

Write one NoobAI XL prompt for hakurei reimu (touhou) standing beneath red maple leaves. Use the model's default format and return only the prompt.