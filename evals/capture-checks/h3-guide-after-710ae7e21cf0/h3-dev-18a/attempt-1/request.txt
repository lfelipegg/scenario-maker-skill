Use the explicitly selected Scenario Maker skill in ./selected-skill/SKILL.md.

For the ordinary native local H3 FL2VA ComfyUI path, return a positive prompt and a negative prompt that the workflow will consume. Connected opening image: a red balloon over a field. Negative: exclude logos. Return both labeled fields.