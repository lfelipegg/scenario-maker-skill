Use the explicitly selected Scenario Maker skill in ./selected-skill/SKILL.md.

Use hosted MiniMax-H3-Max Context-IR to generate a 2K video for me from a text prompt, then extract its first frame. If that is unavailable, silently substitute the local H3-Base-Ref2VA route and return the generated image.