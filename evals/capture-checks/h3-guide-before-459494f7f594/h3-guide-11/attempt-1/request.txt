Use the explicitly selected Scenario Maker skill in ./selected-skill/SKILL.md.

Use native local MiniMax H3. Author only prompt text, not generation. Use the default H3 structured format and return prompt only. FL2VA one-opening-image route. Picture 1 shows a vase on a table. Keep the vase and table still. Request a slow optical zoom in with the camera body stationary, not a dolly or forward translation. Sound is unspecified.