Use the explicitly selected Scenario Maker skill in ./selected-skill/SKILL.md.

Produce two labeled Balanced expansions. A — clothing scope: `a moss-green field coat`. B — isolated-asset scope: `a brass compass with cracked glass`. Keep each result strictly within its stated scope.